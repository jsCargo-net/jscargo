{
  "name": "jscargo",
  "scripts": {
    "types": "cd packages/schema && for f in jscargo-*.schema.json; do bunx json2ts -i \"$f\" -o \"${f%.schema.json}.d.ts\"; done",
    "test": "bun ./packages/test/index.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.4"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
